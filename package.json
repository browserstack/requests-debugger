{
  "name": "networkutilitytool",
  "version": "1.0.0",
  "description": "Tool to debug failed/dropped requests at client side",
  "main": "src/node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pkg": "^4.4.8"
  },
  "dependencies": {
    "winston": "2.4.4"
  }
}
